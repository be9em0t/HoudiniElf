[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tools_Houdini"
version = "0.1.0"
description = "Houdini Agentic Mode tooling for Copilot/GPT integration"
readme = "README.md"
requires-python = ">=3.8"
authors = [
    {name = "Auto-generated", email = "noreply@example.com"}
]

[project.urls]
Home = "https://github.com/..."
